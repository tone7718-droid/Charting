# Changelog

## 2.1.2 - 2026-07-08

### Changed
- 공통 UI 테마를 `therapy_chart/theme.py`로 분리했습니다.
- 메인 화면 배경, 카드, 라벨, 보조 설명, 칩 버튼 색상을 통일했습니다.
- 오른쪽 미리보기를 카드형 문서 영역처럼 보이도록 개선했습니다.
- 복사 버튼을 더 명확한 주요 액션 버튼으로 정리하고, 초기화/설정은 보조 액션으로 낮췄습니다.
- 필수 항목 완료 상태를 오른쪽 상단 배지로 표시합니다. 모든 필수 항목이 채워지면 `✅ 복사 가능`으로 표시됩니다.

## 2.1.1 - 2026-07-08

### Fixed
- VAS 입력을 UI 단계에서 0~10 정수로 제한하고, 출력 로직에서도 같은 범위를 검증합니다.
- 치료시간 설정 입력을 UI 단계에서 1~600분으로 제한하고, 저장 전 보정도 유지합니다.
- 시행횟수를 1~999회차 범위로 제한합니다.
- 직접 수정 모드에서도 시행횟수, 치료시간, VAS가 허용 범위를 벗어나면 복사를 차단합니다.
- 진단코드는 `M75.1`처럼 입력해도 `M751`처럼 점 없는 대문자 표기로 정규화합니다.
- CSV 가져오기/내보내기와 설정 저장 과정에서도 진단코드 정규화와 중복 제거를 적용합니다.

### Changed
- 오른쪽 미리보기 UI를 `therapy_chart/panels/preview_panel.py`로 분리했습니다.
- UI 입력값 검증 공통 함수를 `therapy_chart/ui_validation.py`로 분리했습니다.
- README의 테스트 명령에서 고정 테스트 개수 표기를 제거하고 최신 코드 구조를 반영했습니다.

## 2.1.0 - 2026-07-03

### Added
- 치료 효과 평가 항목을 추가했습니다.
- 주관적 호전도, VAS, 기타 평가 메모를 선택적으로 출력할 수 있습니다.
